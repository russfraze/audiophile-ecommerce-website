import {useContext} from 'react'
import CartContext from '../../context/CartContext'

function Cart() {
    const cartData 
    return (
        <div>
            
        </div>
    )
}

export default Cart
